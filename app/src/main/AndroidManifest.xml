<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.vkdonations">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.VkDonations">
        <activity android:name=".DonationsActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".CreateDonationsOneTime1Activity" />
        <activity android:name=".CreateDonationsOneTime2Activity" />
        <activity android:name=".NewDonationActivity" />
        <activity android:name=".CreateDonationEveryMonthActivity" />
        <activity
            android:name=".CreateDonationFinalActivity"
            android:windowSoftInputMode="adjustResize" />
        <activity android:name=".PostsActivity" />
    </application>

</manifest>